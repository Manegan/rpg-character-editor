import Home from '../app/components/Home.jsx'
import TestUtils from 'react-dom/test-utils'


describe('Home', () => {
  it('should create Home component', function () {
    // var component = TestUtils.renderIntoDocument((<Home />))

    // var h4 = TestUtils.findRenderedDOMComponentsWithTag(component, 'h4')

    // expect(h4.getDOMNode().textContent).toEqual('RPG Character editor!')
  })
})
